import React from 'react';
import { Input } from '@/components/mortalui/index';

const Form  = ( ) => {
  return (
    <div>
      <Input placeholder="Basic usage" />
    </div>
  );
};

export default Form